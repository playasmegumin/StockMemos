# Known Issues

> 记录 StockMemos 中已知的前端展示、逻辑和交互问题。
> 发现新问题后直接追加到对应优先级分组中。

---

> ✅ = 已在 v0.7.1 修复

## P0 — 功能不可用

- [x] ✅ 个股详情页最新价未正常显示 — 按 stock_id 隔离缓存，避免跨股串价
- [x] ✅ 股票列表中浮动盈亏计算错误 — 连锁于前者，已消失

## P1 — 算错 / 显示错

- [x] ✅ 删除股票时出现 API 错误 — Stock 模型追加 StockAnalyze 级联删除关系
- [x] ✅ 基本面数据首次加载有时不展示 — 单股按需拉取替代批量 POST + 门控

## P2 — 体验问题

- [ ] 股票列表和详情页之间切换时偶有数据闪烁 [交互]
- [ ] 添加股票后需手动刷新才看到新数据 [交互]

## P3 — 架构 / 技术债务

- [ ] `_api()` 和 `_fmt_time()` 在两个页面中重复定义 — 应抽离为共享模块
- [ ] session_state key 散落各处，无集中管理 — 存在命名冲突风险
- [ ] 无前端数据层 — API 调用、缓存、错误处理逻辑分散
- [ ] API 响应无类型定义 — 字段名错误时静默失败
- [ ] 前端数据缓存策略未统一（部分用 session_state，部分实时请求）
- [ ] CN/SH/SZ 被视为不同交易所，同一 A 股代码可能被重复添加。CN 本身的语义模糊（"中国"而非"交易所"），应考虑统一 A 股交易所标识为 SH/SZ 或合并为单一 "CN" 源
- [ ] Provider 异常处理链未完全闭环 — 各层（Provider/Service/Frontend）对"数据源无数据"的判断各自为政，未统一为标准的降级/报错协议。已修复 TuShareProvider + MarketDataService 层的空壳穿透，但缺乏跨 Provider 的通用校验契约
